/**
 * Report export and share.
 *
 * Implements **TRD FR-08 Report export & share**: generate and share PDF and DOCX from a
 * completed scan. Accept: both files download and open, and the PDF contains the annotated
 * image, the findings table and both SHA-256 hashes.
 *
 * Every report carries the advisory disclaimer and the rule pack version it was evaluated
 * against (CLAUDE.md §3.6, §3.8).
 *
 * Not implemented yet — P3, against the backend's FR-27.
 */

export {};
